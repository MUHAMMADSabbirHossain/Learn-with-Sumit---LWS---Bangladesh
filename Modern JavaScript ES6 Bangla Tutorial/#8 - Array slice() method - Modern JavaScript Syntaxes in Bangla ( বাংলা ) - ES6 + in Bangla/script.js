// Array.prototype.slice()
var numbers = [1, 2, 3, 4, 5];

// var result = numbers.slice(1, 3); // 2, 3
// var result = numbers.slice(-2, -1);  // 4
var result = numbers.slice(-2, 4); // 4
// var result = numbers.slice(); // [1, 2, 3, 4, 5]
console.log(numbers);
console.log(result); // 