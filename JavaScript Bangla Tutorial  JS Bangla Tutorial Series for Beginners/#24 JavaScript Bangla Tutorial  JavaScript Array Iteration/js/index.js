const numbers = [45, 4, 9, 16, 25];

/* function myFunction(value, index, array) {
    // console.table([value, index, array]);
    console.log(value);
    console.log(index);
    console.log(array);
    console.log("------------------------------")
}

// array method
numbers.forEach(myFunction); */

/* function myFunction(value, index, array) {
    return value * 2;
}

// array map
const newNumbers = numbers.map(myFunction);

console.log(newNumbers); */

/* function myFunction(value, index, array) {
    return value > 10;
}

// array filter
const newNumbers = numbers.filter(myFunction);

console.log(newNumbers) */;


// function myFunction(total, value, index, array) {
//     console.log(total);
//     console.log("----------");
//     return total + value;
//     // return total + (value * value) // square
// }

// // array filter
// const newNumbers = numbers.reduce(myFunction, 10);

// console.log(newNumbers)


/* function myFunction(total, value, index, array) {
    return total + value;
}

// array reduceRight
const newNumbers = numbers.reduceRight(myFunction);

console.log(newNumbers) */


/* function myFunction(value, index, array) {
    return value > 18;
}

// array every
const newNumbers = numbers.every(myFunction);

console.log(newNumbers); */

/* function myFunction(value, index, array) {
    return value > 18;
}

// array some
const newNumbers = numbers.some(myFunction);

console.log(newNumbers); */



/* // array indexOf
const newNumbers = numbers.indexOf(25);

console.log(newNumbers); */


/* // array lastIndexOf
const newNumbers = numbers.lastIndexOf(25);

console.log(newNumbers); */


/* // array includes
const newNumbers = numbers.includes(25);

console.log(newNumbers); */


/* function myfunction(value, index, array) {
    return value > 18
}

// array find
const newNumbers = numbers.find(myfunction);

console.log(newNumbers); */



/* function myfunction(value, index, array) {
    return value > 18
}

// array findIndex
const newNumbers = numbers.findIndex(myfunction);

console.log(newNumbers); */


/* // array from
console.log(Array.from("ABCDEFGH")); */




// array keys
const newNumbers = numbers.keys();

console.log(newNumbers);